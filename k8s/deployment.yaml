apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-backend-deployment
spec:
  selector:
    matchLabels:
      app: demo-backend-deployment
  replicas: 1
  template:
    metadata:
      labels:
        app: demo-backend-deployment
        track: stable
    spec:
      containers:
        - name: demo-backend-deployment
          image: "gcr.io/ml-console-dev/demo-backend-image:latest"
          # resources:
          #   requests:
          #     memory: "1000Mi"
          #     cpu: "150m"
          imagePullPolicy: Always
          readinessProbe:
            httpGet:
              path: /
              port: 3000
          ports:
            - name: demo-backend-port
              containerPort: 3000